<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <div class="navbar-brand">
                <i class="fas fa-code"></i>
                <span>CoreDigify.com</span>
            </div>
            <ul class="navbar-nav">
                <li><a href="/en" class="<%= page === 'home' ? 'active' : '' %>">Home</a></li>
                <li><a href="/en/web-development" class="<%= page === 'webdev' ? 'active' : '' %>">Web Development</a></li>
                <li><a href="/en/contact" class="<%= page === 'contact' ? 'active' : '' %>">Contact</a></li>
                <li class="language-switcher">
                    <% 
                    let lvUrl = '/lv';
                    if (page === 'webdev') lvUrl = '/lv/web-izstrade';
                    else if (page === 'contact') lvUrl = '/lv/kontakti';
                    %>
                    <a href="<%= lvUrl %>" class="lang-link">
                        <i class="fas fa-globe"></i> LV
                    </a>
                </li>
            </ul>
            <div class="navbar-toggle">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </div>
    </nav>

    <main>